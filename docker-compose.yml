services:
  asset-allocation:
    image: ${ASSET_ALLOCATION_IMAGE:-ghcr.io/example-owner/assetallocation:latest}
    env_files:
      - .env
    working_dir: /py_app
    command: ["main.py"]
    restart: "no"
    volumes:
      - ./outputs:/py_app/outputs
      - ./logs:/py_app/logs
